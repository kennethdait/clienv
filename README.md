# clienv
my command line configuration files

# binaries (root/bin)

* status.sh
  * systemReport_macos OUTPUTS:
```
                System Version => macOS 11.2.2 (20D80)
                Kernel Version => Darwin 20.3.0
                   Boot Volume => macos-ssd
                     Boot Mode => Normal
                 Computer Name => SPACEMAN-DEV
                     User Name => Kenneth Dait (kend)
         Secure Virtual Memory => Enabled
   System Integrity Protection => Enabled
               Time since boot => 10 days 6
```
  * systemReport_linux OUTPUTS:
```bash
---
```
  * systemReport_windows OUTPUTS:
```bash
---
```